---
title: "Ubuntu 26.04 세팅(키보드, 터미널, 기타 유틸)"
date: 2026-06-19 06:00:00 +0900
categories: [Ubuntu]
tags: [setup]
---
## 기본 설정
### getid 설치
- https://barrer.tistory.com/69

### 한글
- https://andrewpage.tistory.com/390

### alt_R 비활성화
- https://ashton0410.tistory.com/172

### 기타 유틸 설치
```
sudo apt update && sudo apt upgrade -y
sudo apt install tig
sudo apt install net-tools
sudo apt install curl
```

## Git
### 설치
```
sudo apt-get install git
sudo apt install git
```

### 설정 
```
git config --global user.name jinseopkim
git config --global user.mail tjq2702@gmail.com
```

## 터미널 설정
### zsh 설치
```
sudo apt update 
sudo apt install zsh
```

### oh-my-zsh 설치
```
sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
```

### Powerlevel10k 테마 설치
```
git clone --depth=1 https://github.com/romkatv/powerlevel10k.git ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/themes/powerlevel10k
```

```
vi ~/.zshrc
ZSH_THEME="powerlevel10k/powerlevel10k"
```
#### powerlevel10k 재 설정 명령어
```
p10k configure
```

### pluigin 설치
```
# zsh-syntax-highlighting
git clone https://github.com/zsh-users/zsh-syntax-highlighting.git ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-syntax-highlighting

# zsh-autosuggestions
git clone https://github.com/zsh-users/zsh-autosuggestions ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-autosuggestions

# fzf (Fuzzy Finder )
git clone --depth 1 https://github.com/junegunn/fzf.git ~/.fzf
bash ~/.fzf/install​
```

```
vi ~/.zshrc
plugins=(
git
sudo
colored-man-pages
zsh-syntax-highlighting
zsh-autosuggestions
fzf
)​
```

